{
  "name": "drag_drop_project",
  "version": "1.0.0",
  "description": "learning typescript by making a drag and drop app",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --kill-others \"lite-server\" \"tsc -w\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lite-server": "^2.6.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
